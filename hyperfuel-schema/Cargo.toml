[package]
name = "hyperfuel-schema"
version = "4.0.0"
edition = "2021"
description = "schema utilities for hyperfuel"
license = "MPL-2.0"

[dependencies]
anyhow = "1"
polars-arrow = { version = "0.42", features = ["compute_cast"] }
